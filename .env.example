# Comunicação interna com o backend (porta 3001).
# Use HTTP (NÃO HTTPS) para evitar erros críticos de comunicação interna entre serviços.
# Não use domínio público aqui (ex.: https://meu-dominio.com) — mantenha como endpoint interno/local.
BACKEND_URL=http://127.0.0.1:3001
